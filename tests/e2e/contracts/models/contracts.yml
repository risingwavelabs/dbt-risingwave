version: 2

models:
  - name: contract_orders
    config:
      contract:
        enforced: true
    columns:
      - name: id
        data_type: integer
      - name: payload
        data_type: varchar

  - name: bad_contract_orders
    config:
      contract:
        enforced: true
    columns:
      - name: id
        data_type: integer
      - name: payload
        data_type: integer
